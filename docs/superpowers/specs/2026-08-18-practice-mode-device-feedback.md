# Practice-mode device feedback

**Date:** 2026-08-18  
**Status:** queued for refinement

## Confirmed working

- Local Alice sign-in
- Chat opening
- Normal / Practice switch
- Chat → Translation preferences entry point

## Sending and translated bubble

- Sending English `hi` gave no visible translating state, so the learner could not tell what was happening or which language was being learned.
- A Ukrainian translated outgoing bubble did not wrap naturally; the English/Normal version did wrap.

## Translation preferences

- Match `Not set` to the existing right-side setting-value style used by `Learning language → Ukrainian`; it currently looks too small and like a different typographic treatment.
- Give the picker sheet more top padding, matching the existing Profile → Interface language picker.
- Replace or clarify the unexplained term `Grammatical forms` in the row-level copy.
- Shorten `How you speak with Bob Local`; use `Tone`.
- Remove section subtitles (`Grammatical forms`, `Conversation tone`) if their meaning can live in the rows. The preference screen should be quieter and use fewer levels of hierarchy.
